import { CircularProgress } from '@mui/material';

type Props = {};

const SubjectsLoading = (props: Props) => {
  return (
    <div>
      <CircularProgress />
    </div>
  );
};

export default SubjectsLoading;
